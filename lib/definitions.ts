export type Ship = {
    id: string,
    name: string,
    size: number,
    qfuel: number
};

export type Drive = {
    id: string,
    name: string,
    fuel_use: number,
    speed: number,
    stage1: number,
    stage2: number
};